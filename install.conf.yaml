# Settings
- defaults:
    link:
        create: true
        relink: true
        force: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.zshrc: zsh/zshrc.zsh
    ~/.config/nvim: nvim
    # Git
    ~/.gitconfig: git/gitconfig
