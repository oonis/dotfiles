[user]
    name = Sam Macaluso
    email = sam@macalu.so

[alias]
    update = add -u .
    unstage = reset HEAD
    untracked = status -u .

[mergetool]
    path = /usr/local/bin/nvim
    prompt = false

[diff]
    renames = copy
    tool = nvimdiff
